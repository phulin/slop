# Circuit Target Export: `L19:731 -> L24:3165`

Common prompts across all nodes: `15`

These are intervention targets, not confirmed circuit evidence. Each prompt group should be tested by ablating or clamping upstream nodes and rescanning downstream SAE activations.

## nr_train_01082_01_15d41475be2f

- `L19:731` model `glm-5.2` token `284` activation `6.484375` impact `0.12646484375`: Here is an Arduino function designed to efficiently draw a monochrome bitmap onto an SPI-connected OLED screen using the popular Adafruit GFX library. This function assumes you are using an OLED display object named display. It takes two arguments: a pointe...
- `L24:3165` model `human` token `381` activation `8.8046875` impact `0.24267578125`: Here is code that, when given a bitmap, draws said bitmap onto an OLED display: include <SPI.h> include <AdafruitGFX.h> include <AdafruitSSD1306.h> define OLEDCS 10 define OLEDDC 9 define OLEDRESET 8 define SCREENWIDTH 128 define SCREENHEIGHT 64 // Create a...

## nr_test_00431_01_1abfffe4987e

- `L19:731` model `glm-5.2` token `407` activation `6.6171875` impact `0.0634765625`: The problem is your swap logic at the top of the script. Your code is supposed to count the overlap between the end of the first string and the start of the second. However, the code swaps the variables if string2 is shorter than string1. Because you swap t...
- `L24:3165` model `human` token `511` activation `8.34375` impact `0.220703125`: The second for loop ends too early before it checks every position in the longer string to see if an overlap starts there. If you replace the len(string1) with len(string2) in the range of the for loop, it will continue iterating until it finds the real ove...

## nr_train_06019_01_a7e76a91b00e

- `L19:731` model `gpt-5.5` token `503` activation `6.1875` impact `0.079833984375`: function firstTrueIndex<T>( items: readonly T[], partition: (item: T, index: number, array: readonly T[]) => boolean ): number { let low = 0; let high = items.length; // exclusive while (low < high) { const mid = low + Math.floor((high - low) / 2); if (part...
- `L24:3165` model `human` token `504` activation `10.21875` impact `0.19921875`: Here is a TypeScript function that uses a binary search to find the index of first item for which a partition callback returns true: export function binaryFindPartition<T>( array: readonly T[], partition: (item: T) => boolean ): number { if (array.length ==...

## nr_train_07477_01_4acf4ae69f84

- `L19:731` model `gemini-3.5-flash` token `378` activation `5.9609375` impact `0.2021484375`: The bug in your code is that it sums up all the numbers in the string instead of performing the actual mathematical operation ($+$ or $-$) between the two specific numbers. It also returns a string (like "+15") instead of the calculated integer result. To f...
- `L24:3165` model `human` token `433` activation `11.0390625` impact `0.0166015625`: It looks like the way you are storing the digits after you extract them from the string is the wrong approach. def calculate(string): num = [] # use a list to store the numbers for word in string.split(): if word == 'loses': operator = '-' elif word == 'gai...

## nr_train_04035_01_d3e2dced1b4d

- `L19:731` model `human` token `389` activation `6.515625` impact `0.1064453125`: I'm happy to help with that! To accomplish what you asked for, we first create a set so we can track whether we've already added an element or not. Then, we will use the filter method, which will only pass through elements that evaluate to true in a given f...
- `L24:3165` model `human` token `511` activation `11.1796875` impact `0.0537109375`: I'm happy to help with that! To accomplish what you asked for, we first create a set so we can track whether we've already added an element or not. Then, we will use the filter method, which will only pass through elements that evaluate to true in a given f...

## nr_train_07553_01_9d8bc106909b

- `L19:731` model `human` token `478` activation `6.42578125` impact `0.10302734375`: Here's a fully type-safe asynchronous Array#map function written in TypeScript. async function asyncArrayMap<Element, Return>( array: Element[], callback: (item: Element, index: number, array: Element[]) => Return Promise<Return> ): Promise<U[]> { const res...
- `L24:3165` model `gpt-5.5` token `507` activation `11.4140625` impact `-0.0458984375`: export async function asyncMap<T, U>( items: readonly T[], callback: (item: T, index: number, array: readonly T[]) => U Promise<U>, ): Promise<U[]> { return Promise.all(items.map(callback)); } Usage: const nums = [1, 2, 3] as const; const strings = await as...

## nr_train_07943_01_48c820930ef3

- `L19:731` model `qwen3.6-35b` token `394` activation `5.71484375` impact `0.114990234375`: This Python function extracts the file extension from a given filename string. First, it uses the rpartition method to split the string at the last dot, ensuring accurate handling of filenames with multiple dots. The method returns a tuple containing the pa...
- `L24:3165` model `human` token `471` activation `8.3828125` impact `0.00927734375`: def filenameextension(file): Split the file name into a tuple containing everything up to the last instance of '.', the string '.', and everything after the last instance of '.' parts = file.rpartition('.') If '.' is indeed present in the file name and its...

## nr_train_07473_01_40b4d87e0dc7

- `L19:731` model `gpt-5.5` token `372` activation `6.1796875` impact `0.095703125`: public static int[] runningSum(int[] nums) { int[] result = new int[nums.length]; int sum = 0; for (int i = 0; i < nums.length; i++) { sum += nums[i]; result[i] = sum; } return result; } This function calculates the running sum of a one-dimensional integer...
- `L24:3165` model `gpt-5.5` token `313` activation `9.6796875` impact `-0.0146484375`: public static int[] runningSum(int[] nums) { int[] result = new int[nums.length]; int sum = 0; for (int i = 0; i < nums.length; i++) { sum += nums[i]; result[i] = sum; } return result; } This function calculates the running sum of a one-dimensional integer...

## nr_train_02382_01_c8857fff55ab

- `L19:731` model `gemini-3.5-flash` token `390` activation `5.99609375` impact `0.083984375`: To convert your LaTeX table into a Pandas DataFrame, you can represent the structured data as a dictionary where the "Code" column acts as the headers (keys), and the subsequent rows represent the data. Here is the complete Python code to achieve this: impo...
- `L24:3165` model `glm-5.2` token `491` activation `9.46875` impact `0.008056640625`: Of course! Converting LaTeX tabular data into a Pandas DataFrame is straightforward. For this, we can use the pandas.readcsv function. It includes highly flexible parsing parameters that allow it to cleanly interpret the LaTeX formatting. You can pass the L...

## nr_train_09115_01_0d7c544c93ec

- `L19:731` model `human` token `511` activation `5.65625` impact `0.06640625`: Sure, that's quite simple. We can use the abs function in C's standard library like this: include <stdio.h> include <stdlib.h> int absmax(int arr, int len) { int n = abs(arr[0]); for(int i = 1; i < len; i++) { if(abs(arr[i]) > n) n = abs(arr[i]); } return n...
- `L24:3165` model `human` token `321` activation `9.359375` impact `-0.005859375`: Sure, that's quite simple. We can use the abs function in C's standard library like this: include <stdio.h> include <stdlib.h> int absmax(int arr, int len) { int n = abs(arr[0]); for(int i = 1; i < len; i++) { if(abs(arr[i]) > n) n = abs(arr[i]); } return n...

## nr_train_09092_01_44f450282341

- `L19:731` model `gpt-5.5` token `421` activation `5.70703125` impact `0.060546875`: import cmath import math def printcomplexarctangent(realpart, imaginarypart): """ Takes the arctangent of a complex number and prints the result only if both the real and imaginary parts are finite and not NaN. """ z = complex(realpart, imaginarypart) resul...
- `L24:3165` model `gpt-5.5` token `445` activation `9.4453125` impact `0.0107421875`: import cmath import math def printcomplexarctangent(realpart, imaginarypart): """ Takes the arctangent of a complex number and prints the result only if both the real and imaginary parts are finite and not NaN. """ z = complex(realpart, imaginarypart) resul...

## nr_train_02163_01_144dc94cfeb2

- `L19:731` model `glm-5.2` token `178` activation `5.8203125` impact `0.0205078125`: Of course! The Atbash cipher is a classic monoalphabetic substitution cipher, originally used for the Hebrew alphabet. Its mechanism is beautifully simple: it reverses the alphabet. The first letter maps to the last, the second to the second-to-last, and so...
- `L24:3165` model `glm-5.2` token `328` activation `8.9140625` impact `0.0029296875`: Of course! The Atbash cipher is a classic monoalphabetic substitution cipher, originally used for the Hebrew alphabet. Its mechanism is beautifully simple: it reverses the alphabet. The first letter maps to the last, the second to the second-to-last, and so...

## nr_train_00832_01_950af1d44a60

- `L19:731` model `glm-5.2` token `240` activation `6.09375` impact `0.015869140625`: Of course. Here is a custom React hook named useMergeState. It provides a function that accepts either a partial state object or an updater function. This hook is designed to enhance your state management within functional components. Using TypeScript, we d...
- `L24:3165` model `glm-5.2` token `375` activation `10.703125` impact `-0.005615234375`: Of course. Here is a custom React hook named useMergeState. It provides a function that accepts either a partial state object or an updater function. This hook is designed to enhance your state management within functional components. Using TypeScript, we d...

## nr_train_03767_01_e4fc56557106

- `L19:731` model `human` token `430` activation `7.546875` impact `0.00732421875`: This is a method that will calculate the rounded integer square root of an integer, x. Let's break down each part of the return statement. The bisectright method will take an input list as a parameter, which in this case, the list is a list of integers from...
- `L24:3165` model `gpt-5.5` token `337` activation `10.3203125` impact `0.00048828125`: This method returns the largest integer whose square is less than or equal to x — in other words, it computes floor(sqrt(x)). def calculate(self, x: int) -> int: return bisect.bisectright(range(x + 1), x, key=lambda m: m m) - 1 Step by step: range(x + 1) re...

## nr_train_02154_01_3c5cddd5c7b9

- `L19:731` model `glm-5.2` token `234` activation `6.03125` impact `-0.0020751953125`: Python Turtle GUI Troubleshooting You've made a great start with Python's turtle graphics library! Let me help you fix the issues you're experiencing with your cursor program. Problem 1: Hitting the Edge Too Early The main issue with not reaching the edge i...
- `L24:3165` model `glm-5.2` token `305` activation `9.7890625` impact `0.0023193359375`: Python Turtle GUI Troubleshooting You've made a great start with Python's turtle graphics library! Let me help you fix the issues you're experiencing with your cursor program. Problem 1: Hitting the Edge Too Early The main issue with not reaching the edge i...
