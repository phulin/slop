# Circuit Target Export: `L19:1878 -> L24:3759`

Common prompts across all nodes: `8`

These are intervention targets, not confirmed circuit evidence. Each prompt group should be tested by ablating or clamping upstream nodes and rescanning downstream SAE activations.

## nr_train_01082_01_15d41475be2f

- `L19:1878` model `human` token `511` activation `8.1953125` impact `-0.347412109375`: Here is code that, when given a bitmap, draws said bitmap onto an OLED display: include <SPI.h> include <AdafruitGFX.h> include <AdafruitSSD1306.h> define OLEDCS 10 define OLEDDC 9 define OLEDRESET 8 define SCREENWIDTH 128 define SCREENHEIGHT 64 // Create a...
- `L24:3759` model `human` token `388` activation `11.8359375` impact `-0.0009765625`: Here is code that, when given a bitmap, draws said bitmap onto an OLED display: include <SPI.h> include <AdafruitGFX.h> include <AdafruitSSD1306.h> define OLEDCS 10 define OLEDDC 9 define OLEDRESET 8 define SCREENWIDTH 128 define SCREENHEIGHT 64 // Create a...

## nr_train_09092_01_44f450282341

- `L19:1878` model `gemini-3.5-flash` token `429` activation `7.77734375` impact `0.15625`: Here is a complete Python 3 solution that calculates the complex arctangent using the built-in cmath library and validates the result before printing. import cmath import math def calculatecomplexarctan(): try: Prompt the user for the real and imaginary com...
- `L24:3759` model `gpt-5.5` token `327` activation `12.4453125` impact `0.00732421875`: import cmath import math def printcomplexarctangent(realpart, imaginarypart): """ Takes the arctangent of a complex number and prints the result only if both the real and imaginary parts are finite and not NaN. """ z = complex(realpart, imaginarypart) resul...

## nr_train_07553_01_9d8bc106909b

- `L19:1878` model `human` token `436` activation `7.859375` impact `-0.0673828125`: Here's a fully type-safe asynchronous Array#map function written in TypeScript. async function asyncArrayMap<Element, Return>( array: Element[], callback: (item: Element, index: number, array: Element[]) => Return Promise<Return> ): Promise<U[]> { const res...
- `L24:3759` model `human` token `504` activation `11.75` impact `-0.07275390625`: Here's a fully type-safe asynchronous Array#map function written in TypeScript. async function asyncArrayMap<Element, Return>( array: Element[], callback: (item: Element, index: number, array: Element[]) => Return Promise<Return> ): Promise<U[]> { const res...

## nr_train_09115_01_0d7c544c93ec

- `L19:1878` model `human` token `469` activation `8.765625` impact `0.1240234375`: Sure, that's quite simple. We can use the abs function in C's standard library like this: include <stdio.h> include <stdlib.h> int absmax(int arr, int len) { int n = abs(arr[0]); for(int i = 1; i < len; i++) { if(abs(arr[i]) > n) n = abs(arr[i]); } return n...
- `L24:3759` model `gemini-3.5-flash` token `376` activation `12.0625` impact `0.013671875`: Here are two efficient C functions to find the absolute maximum and minimum values in an integer array. Absolute values are computed using the standard abs() function from <stdlib.h>. include <stdlib.h> include <limits.h> // Finds the absolute maximum value...

## nr_train_04343_01_cbdd564ec621

- `L19:1878` model `gpt-5.5` token `341` activation `8.25` impact `0.06640625`: Here’s a simple Python function that converts a Roman numeral string into an integer. Since you do not need to validate whether the Roman numeral is properly formed, the function can focus only on calculating the value. def romantoint(roman): values = { "I"...
- `L24:3759` model `qwen3.6-35b` token `468` activation `11.9765625` impact `0.0009765625`: To convert a Roman numeral into an integer in Python, you must account for the specific rules governing subtractive notation. Unlike standard addition, where every symbol contributes positively to the total, Roman numerals occasionally place a smaller value...

## nr_train_00047_01_4ca883564914

- `L19:1878` model `gpt-5.5` token `443` activation `7.7421875` impact `-0.0458984375`: You can make a bar chart using matplotlib in Python. Here’s a simple example: import matplotlib.pyplot as plt list1 = ["USA", "China", "Russia", "Brazil", "Japan"] list2 = [22, 53, 32, 45, 56] plt.bar(list1, list2) plt.xlabel("Country") plt.ylabel("Value")...
- `L24:3759` model `human` token `507` activation `12.328125` impact `-0.013671875`: There is a useful module called MatPlotLib that can help you achieve this. If you have not already, you will need to install the module first. To install it with PIP, open your command line and enter: pip install matplotlib To install it with Conda, enter:...

## nr_train_02511_01_2cab94ecb042

- `L19:1878` model `gemini-3.5-flash` token `420` activation `8.15625` impact `0.0263671875`: Here is a complete Python script that implements your book list organizer. def organizebooks(): booklist = [] print("--- Book List Organizer ---") print("Enter the titles of your books. Type 'done' when you are finished.\n") while True: userinput = input("E...
- `L24:3759` model `qwen3.6-35b` token `336` activation `11.890625` impact `0.005859375`: To achieve the desired functionality, we will create a simple yet effective Python script that interacts with the user through an input loop. The core objective is to accumulate book titles sequentially until a specific termination signal is provided. We be...

## nr_train_05310_01_0b105113c2c7

- `L19:1878` model `human` token `491` activation `7.90234375` impact `0.013671875`: Absolutely! The easiest way is to use an associative array based on char codes. Here's a sample implementation: include <stdio.h> include <stdlib.h> include <stdbool.h> include <string.h> define ARRLENGTH 26 define CHARDIFF 97 bool compare(int arr1, int arr...
- `L24:3759` model `gemini-3.5-flash` token `471` activation `12.53125` impact `0.01025390625`: Here is a efficient C function to determine if two strings are anagrams, along with a simple main function to demonstrate its usage. This approach uses a frequency lookup table (an array of integers) to count the occurrences of each character. Since it only...
